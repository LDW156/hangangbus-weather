# 한강버스 통합 대시보드 데모 배포 안내

## 1. 현재 상태

이 패키지는 API 키 없이도 GitHub Pages에 바로 배포할 수 있습니다.
기본값은 `DATA_MODE: 'demo'`이며 정상·주의·운항불가 시나리오를 화면에서 전환할 수 있습니다.

구현 항목:

- 동부선·서부선 운항 가능 여부
- 잠수교 통과높이 자동 계산
- 잠수교 정상·운항주의·운항불가 판정
- 팔당댐 유입량·방류량·추이
- 기상특보·예비특보·내부 사전경보
- 서부선·동부선 예상 강수량
- 8개 선착장 풍향·풍속·최대순간풍속
- 잠수교·한강대교 수위
- 인천 만조·간조·대조기·중첩위험
- 모바일 화면과 PWA 설치

## 2. GitHub Pages 배포

1. 저장소 루트에 이 폴더 안의 파일을 업로드합니다.
2. GitHub 저장소의 `Settings` → `Pages`로 이동합니다.
3. `Deploy from a branch`를 선택합니다.
4. 브랜치는 `main`, 폴더는 `/root`를 선택합니다.
5. 저장 후 표시되는 Pages 주소로 접속합니다.

기존 저장소에 적용할 때는 먼저 현재 파일을 백업하십시오.

## 3. 실시간 API 전환

`config.js`를 수정합니다.

```javascript
DATA_MODE: 'live',
API_URL: 'Google Apps Script 웹 앱 URL',
SHOW_DEMO_CONTROLS: false,
```

라이브 API 호출 실패 시 화면은 데모 데이터로 자동 전환되며 오류 배너를 표시합니다.

## 4. Google Apps Script 배포

1. Apps Script 새 프로젝트를 만듭니다.
2. `google-apps-script/Code.gs` 내용을 붙여넣습니다.
3. 프로젝트 설정에서 `appsscript.json` 표시를 활성화하고 내용을 교체합니다.
4. 스크립트 속성에 우선 `DEMO_MODE=true`를 등록합니다.
5. `배포` → `새 배포` → `웹 앱`을 선택합니다.
6. 실행 사용자는 본인, 액세스 권한은 대시보드 사용 범위에 맞춰 선택합니다.
7. 생성된 웹 앱 URL을 `config.js`의 `API_URL`에 입력합니다.

## 5. 수문 실데이터 연결용 스크립트 속성

실데이터 전환 전 공식 API 문서와 실제 응답을 확인한 뒤 아래 값을 등록합니다.

- `DEMO_MODE=false`
- `HRFCO_SERVICE_KEY=...`
- `HRFCO_WATERLEVEL_URL_TEMPLATE=...`
- `HRFCO_DAM_URL_TEMPLATE=...`
- 추후 `KMA_*`, `KHOA_*` 키와 엔드포인트

URL 템플릿에서는 `{key}`, `{obsCode}`를 사용할 수 있습니다.

예시 형식만 제시하면:

```text
https://.../{key}/waterlevel/.../{obsCode}.xml
```

공식 문서에서 확인되지 않은 주소를 그대로 운영에 사용하지 마십시오.

## 6. 현재 판정 기준

`config.js`에 모여 있습니다.

- 잠수교 주의: 수위 4.10m 이상 또는 통과높이 7.66m 이하
- 잠수교 운항불가: 수위 4.46m 이상 또는 통과높이 7.30m 이하
- 동부선 팔당댐: 2,000㎥/s 이상이며 증가 추세
- 서부선 팔당댐: 3,000㎥/s 이상

풍속·강수 판정값은 데모용 초안입니다. 회사 확정 기준으로 수정하십시오.

## 7. 데이터 계약

프론트엔드는 Apps Script가 아래 최상위 구조로 JSON을 반환한다고 가정합니다.

```json
{
  "meta": {},
  "alerts": [],
  "hydrology": {
    "jamsuBridge": {},
    "hangangBridge": {},
    "paldang": {}
  },
  "weather": {
    "rainfall": {"west": {}, "east": {}},
    "windStations": []
  },
  "tide": {}
}
```

## 8. 다음 실작업

1. 현재 수문 API 원본 XML 1회분 확보
2. 잠수교·한강대교·팔당댐 XML 태그 확정
3. Apps Script 수문 어댑터 완성
4. 기상청 특보·단기예보 연결
5. 조석 API 연결
6. 알림과 이력 저장 추가

## 9. V2 시간축 및 비교 기준

모든 화면값에는 `관측시각`, `예보 발표시각` 또는 `이벤트 시각`을 함께 표시합니다.

| 데이터 | 화면 비교 간격 | 표시값 |
|---|---:|---|
| 잠수교·한강대교 수위 | 10분 | 현재, 10분 전, 30분 전, 1시간 전 |
| 팔당댐 유입·방류 | 10분 | 현재, 10분 전, 30분 전, 1시간 전 |
| 풍향·풍속 관측 | 10분 | 현재, 10분 전, 1시간 전 |
| 풍향·풍속 예보 | 1시간 | 1시간 후, 3시간 후 |
| 예상 강수량 | 1시간 | 과거 관측, 현재, 시간별 예보 |
| 기상특보 | 발표·발효 시각 | 발표시각과 발효예정시각 분리 |
| 조석 | 만·간조 이벤트 | 이전 만·간조, 기준시각, 다음 만·간조 |

### 수문 history 계약

```json
{
  "observedAt": "2026-07-15T10:00:00+09:00",
  "intervalMinutes": 10,
  "history": [
    {"time":"09:50","timestamp":"2026-07-15T09:50:00+09:00","value":4.21},
    {"time":"10:00","timestamp":"2026-07-15T10:00:00+09:00","value":4.22}
  ]
}
```

### 풍속 계약

각 선착장은 현재 관측값과 `previous10m`, `previous1h`, `forecast1h`, `forecast3h`를 반환합니다. 관측과 예보를 화면에서 명확히 구분합니다.
