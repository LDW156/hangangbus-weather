(() => {'use strict';const clock=document.getElementById('portalClock');const userEl=document.getElementById('portalAccessUser');const roleEl=document.getElementById('portalAccessRole');const dot=document.querySelector('.portal-access-state .auth-dot');const tick=()=>{if(clock)clock.textContent=new Intl.DateTimeFormat('ko-KR',{hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:false}).format(new Date());};tick();setInterval(tick,1000);const apply=me=>{if(!userEl||!roleEl)return;if(me?.authenticated&&me?.user){userEl.textContent=me.user.name||me.user.user_id||me.user.email||'승인 사용자';roleEl.textContent=`${me.user.role||'USER'} · ${me.user.organization||'한강버스'}`;dot?.classList.add('online');}else{userEl.textContent='인증 연동 대기';roleEl.textContent='현재 포털 구조 확인 단계';}};window.HanGangAuth?.getMe?window.HanGangAuth.getMe().then(apply).catch(()=>apply(null)):apply(null);})();